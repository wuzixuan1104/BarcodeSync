import styled from 'styled-components';

const WrapperSyle = styled('div')`
  display: flex;
  background: #cab089;
  font-weight: bold;
`;

const MenuStyle = styled('div')`
  flex: 1;
`;

export { WrapperSyle, MenuStyle };
